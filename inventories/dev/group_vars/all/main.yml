keycloak_base_url: "https://keycloak.example.com"
external_idp_base_url: "https://idp.example.com/affwebservices/CASSO/oidc/client-apaas-dev"

broker_realm:
  realm_name: "dev-broker"
  display_name: "Broker Realm"
  internal_idp:
    # Client for Broker Realm / Identity Provider for
    display_name: "Team SSO"
    description: "Client for team realms to broker identity"

    client_id: "team-sso"
    client_authenticator_type: "client-secret-jwt"

    base_url: ""
    root_url: "{{ keycloak_base_url }}/realms/{{ broker_realm.realm_name }}/"
    admin_url: "{{  keycloak_base_url }}/admin/{{ broker_realm.realm_name }}/console/"
    redirect_uris:
      - "{{ keycloak_base_url }}/realms/*"
    web_origins:
      - "{{ keycloak_base_url }}"

  external_idp:
    # Identity Provider configuration to broker requests to
    alias: "external-idp-oidc"
    display_name: "External Corporate IdP"
    base_url: "{{ external_idp_base_url }}"
    metadata_descriptor_url: "{{ external_idp_base_url }}/.well-known/openid-configuration"
    issuer_url: "{{ external_idp_base_url }}"
    authorization_url: "{{ external_idp_base_url }}/authorize"
    token_url: "{{ external_idp_base_url }}/token"
    jwks_url: "{{ external_idp_base_url }}/jwks"
    token_introspection_url: "{{ external_idp_base_url }}/introspect"
    user_info_url: "{{ external_idp_base_url }}/userinfo"
    logout_url: ""

    client_id: "0003cc96-bf62-1a21-a278-63260ad30000"
    client_secret: "{{ vault_broker_realm.external_idp.client_secret }}"
    client_auth_method: "client_secret_post"
    sync_mode: "FORCE"
    default_scope:
      - "openid"
      - "profile"
      - "example"
      - "aud-apaas-dev"
      - "groups"
    mappers:
      - claim: "groups"
        user_attribute: "external_groups"
        sync_mode: "INHERIT"
        multivalued: true
      - claim: "https://example/company"
        user_attribute: "noInstitution"
        sync_mode: "INHERIT"
        multivalued: false

team_realm_defaults:
  clients:
    public_client: true
    auth_type: "client-secret"

    flows:
      standard: true
      implicit: false
      direct_access: false
      service_account: false

  internal_idp:
    alias: "broker-realm"
    display_name: "Broker Realm"
    metadata_descriptor_url: "{{ keycloak_base_url }}/realms/{{ broker_realm.realm_name }}/.well-known/openid-configuration"
    issuer_url: "{{ keycloak_base_url }}/realms/{{ broker_realm.realm_name }}/"
    authorization_url: "{{ keycloak_base_url }}/realms/{{ broker_realm.realm_name }}/authorize"
    token_url: "{{ keycloak_base_url }}/realms/{{ broker_realm.realm_name }}/token"
    jwks_url: "{{ keycloak_base_url }}/realms/{{ broker_realm.realm_name }}/jwks"
    token_introspection_url: "{{ keycloak_base_url }}/realms/{{ broker_realm.realm_name }}/introspect"
    user_info_url: "{{ keycloak_base_url }}/realms/{{ broker_realm.realm_name }}/userinfo"
    logout_url: "{{ keycloak_base_url }}/realms/{{ broker_realm.realm_name }}/logout"

    client_id: "{{ broker_realm.internal_idp.client_id }}"
    client_auth_method: "client_secret_jwt"
    sync_mode: "FORCE"
